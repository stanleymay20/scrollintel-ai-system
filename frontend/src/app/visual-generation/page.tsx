'use client'

import { VisualGenerationInterface } from '@/components/visual-generation/visual-generation-interface'

export default function VisualGenerationPage() {
  return <VisualGenerationInterface />
}